# ── Backend Environment Variables ─────────────────────────────────────────────
# HOW TO USE:
#   1. Copy this file → rename to .env for local dev
#   2. On Google Cloud Run → paste each variable in the Cloud Run env vars UI

# ── Database (Neon.tech FREE PostgreSQL) ──────────────────────────────────────
# 1. Go to https://neon.tech → Sign up free
# 2. Create a project called "wealth-management"
# 3. Copy the connection string from the Dashboard → it looks like below
DATABASE_URL=postgresql://neondb_owner:YOURPASSWORD@ep-XXXX.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# ── Redis (Upstash FREE Redis) ────────────────────────────────────────────────
# 1. Go to https://upstash.com → Sign up free
# 2. Create a Redis database → region: ap-south-1 (Mumbai, closest to India)
# 3. Copy the "Redis URL" from the console → it looks like below
REDIS_URL=rediss://default:YOURPASSWORD@YOUR-INSTANCE.upstash.io:6379

# ── Security ──────────────────────────────────────────────────────────────────
# Generate your own: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=REPLACE_WITH_YOUR_OWN_RANDOM_64_CHAR_STRING
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ── Alpha Vantage API (Free market data) ─────────────────────────────────────
# Get free key at: https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY=YOUR_ALPHA_VANTAGE_API_KEY

# ── CORS: Your frontend Cloud Run URL (fill in AFTER deploying frontend) ──────
FRONTEND_URL=https://YOUR_FRONTEND_CLOUD_RUN_URL
